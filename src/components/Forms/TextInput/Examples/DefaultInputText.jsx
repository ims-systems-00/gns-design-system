import React from "react";
import Input from "../InputBase";

const DefaultInputText = ({}) => {
  return <Input type="text" name="text" placeholder="Some text value..." />;
};

export default DefaultInputText;
