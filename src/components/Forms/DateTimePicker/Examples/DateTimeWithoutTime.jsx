import DateTime from "../DateTimeBase";

const DateTimeWithoutTime = ({}) => {
  return (
    <>
      <DateTime
        timeFormat={false}
       />
    </>
  );
};

export default DateTimeWithoutTime;
